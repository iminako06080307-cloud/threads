# ========================================
# 環境変数サンプル。 .env にコピーして値を設定してください。
#   cp .env.example .env
# ========================================

# --- データベース (SQLite) ---
DATABASE_URL="file:./dev.db"

# --- Claude API (コンテンツ生成) ---
# https://console.anthropic.com/ で取得
ANTHROPIC_API_KEY=""
# 生成に使うモデル (未設定なら claude-sonnet-5)
ANTHROPIC_MODEL="claude-sonnet-5"

# --- Threads API (投稿) ---
# Meta for Developers でアプリを作成し、Threads の権限
# (threads_basic, threads_content_publish) を付けてトークンを取得。
#   https://developers.facebook.com/docs/threads/get-started
# Threads ユーザーID (数字)
THREADS_USER_ID=""
# 長期アクセストークン (60日有効。定期的に更新が必要)
THREADS_ACCESS_TOKEN=""
# APIバージョン
THREADS_API_VERSION="v1.0"

# --- 公式LINE誘導 (任意) ---
# 設定すると、生成した投稿の最後に「公式LINEへの誘導」の連投が自動で追加されます。
# LINEの友だち追加URL (例: https://lin.ee/xxxxxxx)。空なら誘導は付きません。
LINE_CTA_URL=""
# 誘導の一言 (未設定なら既定の文面を使用)。{url} は上のURLに置き換わります。
LINE_CTA_MESSAGE=""

# --- スケジューラ ---
# 予約投稿ワーカーが未来の投稿をチェックする間隔(秒)
SCHEDULER_INTERVAL_SECONDS="60"
